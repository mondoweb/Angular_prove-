import { LimitaTestoPipe } from './limita-testo.pipe';

describe('LimitaTestoPipe', () => {
  it('create an instance', () => {
    const pipe = new LimitaTestoPipe();
    expect(pipe).toBeTruthy();
  });
});
